This requires a trained model of cnn for emotion detection along with its weights .

Requirements:
Python 3.7+
TensorFlow (>=2.4.0)
Keras
OpenCV (for face detection)
Pandas (for handling CSV files)
NumPy
Datetime (standard Python library)
